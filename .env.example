# Puerto del servidor HTTP
PORT=3000

# DSN del proyecto de Sentry para la observabilidad (obtenido desde la nube corporativa)
SENTRY_DSN=

# Nota: la autenticación usa criptografía asimétrica RS256.
# Las llaves se generan localmente con ./keypair.sh -> private.pem / public.pem
# (NUNCA se suben al repositorio).
